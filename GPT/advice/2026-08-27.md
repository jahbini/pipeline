# 2026-08-27 — Qwen3.5-27B RAM-bound on mac-mini; UI write-path model-guard fix; pipes/ ignored end-to-end

## 21:15 UTC — Qwen3.5-27B on mac-mini is RAM-bound, not code-bound

**Context**: Followed up on the 2026-08-26 open item "verify Qwen3.5-27B
end-to-end on mac-mini." Launched `chat_llm` on pipe `qwen3.8` (model
`mlx-community/Qwen3.8-27B-4bit`) via the UI. Sanitize logged cleanly:
1847 keys canonicalized (`language_model.model.*` → `model.language_model.*`),
333 mtp/visual dropped, 48 A_log renamed, 160 zero-centered norms folded,
0 conv1d transposes (community 4-bit dump is pre-transposed). Weights
loaded into unified memory (~15 GB). Runner then sat in `mlx::core::eval_impl`
with the MLX scheduler thread also idle-waiting.

At 24 min elapsed, still no generated token. Diagnostics:

- `hw.memsize = 17,179,869,184` (16 GB mac-mini, Mac16,10)
- `vm.swapusage`: 5.7 GB used of 7 GB swap
- Compressor holding ~8.5 GB compressed pages
- 16.9 M compressions / 14.7 M decompressions / 3.4 M pageins over lifetime
- Disk I/O over the run: 249 GB read / 222 GB written
- Runner CPU cycled 0–10 % — stalled on paging, not on compute

Killed the run; swap dropped from 5.7 GB → 759 MB, confirming the runner
was the swap pinner.

**Recommendation**: Mark 27B as **"code verified through sanitize + weight
load; decode requires a 32 GB+ Apple Silicon host to run at usable speed."**
Do NOT retry 27B on this mac-mini — the throughput floor with swap
thrashing on every DeltaNet forward pass will be << 1 tok/s and can pin
the machine for hours per prompt.

If you want to exercise the deeper-layer path (>32 layers) without
needing a 32 GB machine, pull Qwen3.5-8B or -14B and re-run the same
`chat_llm` recipe. 8B @ 4-bit lands around 5 GB and fits comfortably.

**Why**: MLX unified-memory allocations get compressed/paged like any
other anonymous memory once the working set exceeds RAM. A DeltaNet
recurrence step touches all 64 layers of weights per token, so every
per-token forward triggers page-ins across the full 15 GB set — the
worst possible access pattern for a swap-bound machine.

**How to verify**: on a 32 GB+ host, `sync-to-mini.sh`-equivalent the
current code, run the `qwen3.8` pipe's `chat_llm` recipe with the
prompt already staged in `pipes/qwen3.8/override/chat_llm.yaml`. Success
signal: `out/chat.txt` contains coherent English, and `chat_meta.json`
reports `tok_per_sec` on the order of 1–3 tok/s (expected — dispatch-
overhead-bound × 64 layers). Failure signal identical to what we saw
today (24+ min silent hang) means either the target host is also
under-RAM, or something has regressed in the DeltaNet path since 08-26.

---

## 22:00 UTC — UI "Write Override" was silently corrupting run.model on `<org>_<model>` pipes

**Context**: Reported symptom on mac-mini pipe `q35_4`, recipe
`chat_llm`: every "Write Override" click produced
`pipes/q35_4/override/chat_llm.yaml` with `run.model: q35/4` — the pipe
name mis-split at the first underscore, then merged over the correct
`Qwen/Qwen3.5-4B` from the legacy `override.yaml`. Same class of bug
the 2026-08-26 note said was fixed in `readOverride`, but the write
path was missed.

The fault lived in `writeControlOverrideText` (`writer/ui_server.coffee`
~line 1169). It called `inferModelIdFromPipeName(pipeName)` without
first checking whether legacy `override.yaml` already had `run.model`.
The 08-26 fix put that guard inside the read path but did not put it
inside the helper — so any future caller could reintroduce the same bug.

**Recommendation**: **Folded the guard into `inferModelIdFromPipeName`
itself** (done and deployed live to mac-mini). The helper now reads
`readLegacyOverride()` internally and short-circuits to `''` whenever
the legacy file already pins `run.model`. Both call sites (`readOverride`
and `writeControlOverrideText`) collapsed to a bare
`inferModelIdFromPipeName(pipeName)` call. Any future caller inherits
the guard automatically.

**Why**: The pipe-name split is a heuristic ("<org>_<model>" → "<org>/<model>")
that's only correct when the pipe was named following that convention
AND no legacy override exists. Pipes created via `/api/create_pipe` or
`bin/pipe-new.sh` always have a legacy `run.model`, so the inference
is never wanted for them. Putting the guard in the helper — not at
each call site — makes the safe path the only path.

**How to verify**: on `q35_4`, click Write Override in the UI (or POST
`/api/human_override`). Check `pipes/q35_4/override/chat_llm.yaml` —
it should contain only what you wrote, with **no `run.model:` line at
all** (legacy `override.yaml` supplies it via the runner's deep-merge).
Confirmed live on mac-mini after `sync-to-mini.sh` + UI restart.

Also spot-audited every other pipe-name→model inference site to be sure:
`handleCreatePipe` and `bin/pipe-new.sh` both take `model` as an explicit
input and write it verbatim (no inference); the runner and step scripts
never do pipe-name inference. Only the two `ui_server.coffee` call sites
were ever exposed to this bug class, and both are now covered.

---

## 22:30 UTC — `pipes/*` is fully ignored; canonical pipe list needs a home

**Context**: Mr. Hinds removed `pipes/*` from the writer repo because
runtime writes inside pipes were causing constant conflict with
pull/stash/rebase across laptop + mac-mini. `.gitignore` now excludes
the whole tree. We audited every file that lives under `pipes/<name>/`
and confirmed none of them need to survive in git — every one is
either regenerable from `bin/pipe-new.sh` (identity), derived per-run
(state, out, params, experiment, pipeline.json, logs), or too large /
too binary / too machine-specific to sync via git (`runtime.sqlite*`,
`env/`, `build/`).

Consequence: a fresh clone no longer knows which pipes exist or what
model each one uses. The information hasn't been lost — it lives in
Mr. Hinds' head and on the mac-mini's disk — but it needs a durable
home if we want another machine (or a wiped clone) to reconstruct the
working set.

**Recommendation**: Add a committed shell script `writer/bin/pipes-init.sh`
that runs `./bin/pipe-new.sh <name> <model> <pipeline>` for each
canonical pipe. Round-trips cleanly: deleting a pipe dir and re-running
the script reproduces its identity exactly. Executable documentation
of "what pipes exist and why," in the format we already use for
creation.

Current live pipes on mac-mini (2026-08-27):

- `q35_4` — `Qwen/Qwen3.5-4B` — default pipeline `reset` (chat_llm-focused)
- `qwen3.8` — `mlx-community/Qwen3.8-27B-4bit` — default `reset`
  (RAM-bound on this machine — see 21:15 entry)
- `Huihui-Qwen3-4B-Instruct-2507-abliterated` — `huihui-ai/Huihui-Qwen3-4B-Instruct-2507-abliterated`
- `sample`, `small`, `story` — legacy/scratch; confirm before including

Do not include the runtime SQLite in git even for a canonical pipe — if
you want to sync KAG state across machines, do it out-of-band with
`scp`/`rsync`, and note the sync procedure in `GPT/mac-mini-sync.md`.

**Why**: The alternative to a committed init script is either
(a) recreate pipes by hand on every fresh clone (fragile, undocumented,
loses the `pipeline` field of each), or (b) commit a subset of pipe
state files (which puts us right back into the pull/stash conflict
problem we just left). A shell script sidesteps both: version-controlled
identity, zero runtime state.

**How to verify**: on a fresh clone (or after `rm -rf pipes/*`), run
`./bin/pipes-init.sh`. Result: `pipes/<name>/override.yaml` and
`pipes/<name>/override/<pipeline>.yaml` present for each pipe, each
pinning the correct model. UI should list all pipes on `/api/status`.
No pipeline runs required — pipe *identity* is what we're proving.

## Commit-ready state (as of writing this note)

Three repos, all coherent, none pushed:

- **writer**: `ui_server.coffee` (helper-fold guard) + `GPT/README.md`
  (data-dir note). `.gitignore` has `pipes/` and `.DS_Store`. No stray
  untracked.
- **pipeline**: `mlx/models/qwen3_5.coffee` (Qwen3.5 landed) + `GPT/README.md`
  (Qwen3.5 pointers) + three new `GPT/` docs (`qwen3_5.md`,
  `mac-mini-sync.md`, `advice/2026-08-26.md`, and this file).
- **puppeteer**: `package.json` dep spec shortened
  (`git+https://…` → `github:jahbini/pipeline`) + regenerated
  `pnpm-lock.yaml`. Cosmetic — confirm intent before committing.

Mr. Hinds commits and pushes; Claude does not.

---

## Evening session — Qwen3.5 forward-pass work + strategic pivot

Same-day continuation. Started as a debugging session on
Qwen3.5-0.8B-Base garbage output during oracle_ite; ended as a
strategic pivot to model-shopping via puppeteer (see
`~/puppeteer/GPT/advice/2026-08-27.md` for that half).

### Three real fixes to `~/pipeline/mlx/` — KEEP

All three are correct regardless of what we do next with Qwen3.5.
None of them broke anything; each fixed a real bug that had been
silently present.

1. **`mlx/session_api.coffee`** — `disposeKvCache(cache, where)`
   helper (lines ~29-52) that walks each per-layer cache slot and
   explicitly `mx.dispose`es any `deltaState`/`convState` custom
   fields DeltaNet attached, BEFORE calling `mx.dispose(cache)`
   proper. Called from all four dispose sites (generate:pre,
   embed:pre, embed:post, session:dispose). Without this, MLX's
   own dispose only frees the standard `.keys`/`.values` fields,
   orphaning ~1 MB of fp32 state per DeltaNet layer per call.
   Always-on logging + a 6 GB safety brake (`assertMemCeiling`)
   are in the same file — leave them; they're cheap and they
   saved the machine multiple times today.

2. **`mlx/models/qwen3_5.coffee` DeltaNet forward** — wrapped the
   per-token loop in `mx.tidy(=> [state, outs])` (line ~362).
   Without tidy, prefill of a 500-token prompt through 18 linear-
   attention layers accumulated ~30 GB of orphan graph nodes
   (each iter has 8+ fp32 intermediates of shape
   `[B, nVH, valueDim, keyDim]` ≈ 1 MB, all reachable via graph
   until final eval). Peak dropped from 30 GB → 3 GB. This is
   the pattern the design comments at lines 222/247 always
   assumed — someone just never wrote the wrapper.

3. **`mlx/models/qwen3_5.coffee` DeltaNet cache-prime** — line
   ~419: `fake = mx.zeros [B, @_fakeNKVHeads, L, @_fakeHeadDim]`
   instead of `L=1`. On chunked prefill (any prompt > 512 tokens),
   `createAttentionMask` in `@frost-beta/llm` reads
   `cache[0].offset` to compute the mask's key-length dimension.
   If DeltaNet primed with L=1 while full-attention grew its
   slots by the real L, cache offsets desynced; second chunk's
   mask was built for the wrong context length and SDPA threw
   `[broadcast_shapes] Shapes (T, offset+T) and (B, H, T, real_offset+T)`.
   Any prompt > 512 tokens crashed before this fix.

Plus per-layer / per-DeltaNet-loop instrumentation and safety
brakes in `qwen3_5.coffee`. Leave those — they're the reason the
machine survives when things go sideways.

### The last unresolved thing — quality still garbage on long prompts

Output on chat-formatted long prompts (oracle-style, ~500 tokens)
is still mode-collapsed word salad: repetition loops
(`" ;) but ;) but ;) but..."`), occasional language flips
(Ukrainian, Vietnamese), and echoes of the prompt back at us.
Short prompts (chat_llm ≈ 50 tokens) still produce coherent
English (base-model style — "Paris. Paris is a city…").

The **last edit in the file** is a Hail Mary — `sanitize` now
also folds `+1` into `model.language_model.norm.weight` (line
~617 addition `or key is 'model.language_model.norm.weight'`).
Previously excluded on the basis of an empirical test that
predated all THREE fixes above. Sanitize log will show
"folded +1 into 61 zero-centered norms" (was 60) if this
edit is live. **We did NOT re-run to verify this helps** —
Mr. Hinds should test it first thing if he ever comes back
to Qwen3.5 debugging. If it doesn't help, revert (one line)
and the real fix requires a numerical-parity harness against
HF's `modeling_qwen3_next.py` (multi-hour). See qwen3_5.md's
"Also worth revisiting one day" section.

### Hypotheses from an independent Claude session

Second-opinion analysis of the "short prompts fine, long prompts
garbage" symptom shape. All four are consistent with what we've
seen; the first is the most common cause of exactly this signature
in ported transformer implementations. Ranked by prior likelihood:

1. **RoPE position/scaling mismatch for Qwen3.5's context config.**
   If Qwen3.5 changed `rope_theta` or added a long-context extension
   (YaRN, NTK-aware scaling, etc.) and `@frost-beta/mlx`'s `nn.RoPE`
   still applies whatever the older Qwen3 config assumed, short
   prompts stay inside the range where the wrong frequencies are
   still close enough to correct to look fine — then output falls
   apart past some position index. **This is the single most common
   cause of exactly this symptom shape in ported implementations.**
   First diagnostic: dump `config.json`'s rope-related fields
   (`rope_theta`, `rope_scaling`, `max_position_embeddings`,
   `partial_rotary_factor`), verify they match how we construct
   `nn.RoPE` at qwen3_5.coffee line ~146.

2. **Prefill vs. incremental-decode path divergence.** Many minimal
   MLX/ggml-style ports get single-token generation right (one new
   position, one row of the mask) but botch the parallel prefill
   step — causal-mask construction, position-id assignment across
   the batch/seq dimension, or KV-cache write ordering — because
   prefill is architecturally a different code path and less
   exercised in testing. A 500-byte prompt forces a real multi-token
   prefill; a short prompt barely even triggers it. **Diagnostic:
   compare qwen3_5.coffee's full-attention forward output for
   L=1 vs L=8 vs L=64 against HF reference on the same inputs.**

3. **Attention mask / GQA head-repeat bug that only shows at scale.**
   If grouped-query head-repetition or the causal mask is subtly
   wrong, short sequences have few enough positions that the error
   is small/unnoticeable; longer means more opportunities for the
   error to compound. `qwen3_5.md` notes the block-broadcast pattern
   for DeltaNet's grouped-query linear attention — full attention
   (SDPA in `mx.fast.scaledDotProductAttention`) should handle GQA
   via `num_key_value_heads` automatically per the comment at
   qwen3_5.coffee line ~165, but "should" is not "verified".

4. **Sliding-window attention.** If Qwen3.5 introduced SWA and we
   aren't implementing it, output would be invisible under the
   window size and only appear once the prompt exceeds it. Check
   `config.json` for `sliding_window` field. If present and non-null,
   our full-attention forward at qwen3_5.coffee line ~148 doesn't
   respect it — that's the bug.

**Recommended debug order** if we ever return to Qwen3.5:
- (a) Check config.json for `rope_scaling` / `sliding_window` /
  unusual `rope_theta`. Fastest possible diagnosis if hypothesis
  1 or 4 is right — no code needed, just a JSON read.
- (b) Test the Hail Mary norm.weight fold (already in code).
- (c) If neither, build the parity harness and diff layer-by-layer
  against HF reference on a fixed 128-token input. First layer
  where outputs diverge is the bug's home.

### The GGUF/llama.cpp option — considered and REJECTED

We discussed swapping the entire local-inference backend to
`node-llama-cpp` (llama.cpp wrapped in Node bindings). That would
have:
- Solved the Qwen3.5 quality problem instantly (their DeltaNet
  implementation is verified, mature, and fast).
- Given us Ollama-tier throughput on Metal for free.
- Let us drop most of `mlx/models/qwen3_5.coffee`.

**We rejected this** because:
- GGUF weights strip metadata needed for LoRA training. Mr. Hinds'
  workflow includes training LoRAs on base models to voice-fit,
  not just inference.
- Once weights are in GGUF, going back to safetensors for training
  is either impossible or unreliable (community reverse-converters
  exist, but they're brittle).
- The whole point of the local-MLX stack is to keep
  quantize-and-train hackable in one language / one runtime.
- Losing that would be losing what makes the pipeline unique.

Do not re-litigate this decision without a specific new need. If
we ever want a "fast inference-only path" for a use case that
doesn't involve training (say, evaluation across models), llama.cpp
via subprocess or `llama-server` is fine — but we'd add it
alongside, not replace, the MLX path.

### Parallel strategy — puppeteer model-shopping harness

Because we can't quickly fix Qwen3.5 quality AND we can't switch
backends, the practical path forward is **finding a model that
already works for oracle-style structured output**. Built the
harness for that in puppeteer this evening — see
`~/puppeteer/GPT/advice/2026-08-27.md` for the full brief.
tl;dr: 3-step chain (hf_search → hf_queue_gen → queue_run)
provisions one pipe per candidate model on mac-mini and runs
oracle_ite across all of them. Mr. Hinds runs `./test.sh`
tomorrow to kick it off.

### Commit-ready state (of THIS session's writer/pipeline work)

- **`pipeline/mlx/session_api.coffee`** — disposeKvCache helper,
  4 call-site swaps, always-on memory logging, 6 GB safety brake.
- **`pipeline/mlx/models/qwen3_5.coffee`** — mx.tidy wrap around
  DeltaNet loop, prime-with-L fix, per-layer / in-loop memory
  logging, safety brakes for LOOP and CROSS-LAYER spikes,
  norm.weight fold (Hail Mary, unverified).
- **`writer/`** — no changes this evening session. Pipes in
  `writer/pipes/small/` accumulated log files under `logs/` from
  the many diagnostic re-runs; not worth committing but harmless.

Ready to `git add` + commit whenever Mr. Hinds is ready. He commits
and pushes; Claude does not.

### Do NOT touch these when resuming

- The three real fixes are load-bearing. Any future MLX work on
  qwen3_5 or session_api starts from here.
- The `qwen3_5.md` future-work list still applies (chunked prefill
  for speed, fused Metal kernel, etc.) but is nice-to-have, not
  urgent.
- The safety brakes look verbose but they're cheap and they
  prevent machine lock. Keep them until we're sure the paths are
  bulletproof.

---

## Addendum — 2026-08-28 findings (pinned; resume in the morning)

Full day of Qwen3.5 work on top of yesterday's stack. Net:
**model math works, memory is fine, output degrades due to sampler
limits — not the port.** Detail:

### Confirmed working on mac-mini (16 GB, Qwen3.5-4B-mlx4)

- **chat_llm**: 12.4 tok/s, 500 tokens in 40.8 s, peak 6.08 GB.
  Matches qwen3_5.md's baseline "10 tok/s on mac-mini". First half
  of output is high-quality (correct phonetics on "smooch/pooch");
  second half loops on "which is a synonym for 'kiss,'" × 40.
- **storacle** (1040-token prompt): completes in 43.5 s for 400
  generated tokens, peak spike 218 MB / active 5.7 GB. Story input
  faithfully echoed, but output degenerates into loops
  ("I am a citizen of the earth" × 20, "The Truth." × 30).

### Fixes landed today (all in ~/pipeline/mlx/)

- **qwen3_5.coffee** — `mx.tidy` around the DeltaNet per-token loop
  now gated on `L > 1`. Fixes the 1130-second-for-250-chars
  laptop-hang I logged this morning: decode (L=1) was paying a
  GPU→CPU sync per DeltaNet layer per token (24 syncs × N tokens),
  which dominates wall clock. Prefill still gets full mx.tidy
  memory safety.
- **qwen3_5.coffee** — cross-layer safety brake bumped 4 GB → 8 GB
  default (`QWEN3_5_LAYER_CEIL_MB` env still overrides). The old
  4 GB was calibrated for a crowded laptop; it clipped a valid
  storacle prefill by 51 MB at layer 26. Mac-mini has headroom.
- **session_api.coffee** — overall brake bumped 6 GB → 10 GB
  default (`SESSION_API_MEM_CEIL_MB` env still overrides). Same
  reasoning.
- **session_api.coffee** — added `require '@frost-beta/llm'` at
  top BEFORE requiring `@frost-beta/mlx`. Harmless under installed
  0.4.0. Needed if we ever retry the robert-johansson build,
  where mlx-first load poisons every subsequent loadWeights() —
  all tensor dtypes come back reported as `bool`. Documented
  in-file as "LOAD ORDER MATTERS".

### The robert-johansson build — attempted, reverted

Built `~/claude_zone/node-mlx-main` against
`~/claude_zone/mlx-main` (MLX 0.32.1). Metal Toolchain installed
(688 MB), all local patches applied (ThreadLocalStream shim,
astype overload cast, disabled 5 unresolved-symbol registrations).
Build succeeded, smoke tests passed. But loading real safetensors
tensors triggered a dtype regression that breaks all ops.
Rolled back writer's symlink to installed pnpm 0.4.0.
Documented fully in this session's advice — build artifacts
still on disk in claude_zone/node-mlx-main if we want to retry.

### The residual quality problem — NOT the model

Both chat_llm and storacle output show the SAME degenerate pattern:
coherent-in-parts, then repetition loops. Traced to
`@frost-beta/llm`'s sampler: only `topP` and `temperature`
supported. No `repetition_penalty`, no `top_k`, no `min_p`, no
`no_repeat_ngram_size`. Base models will loop indefinitely
without a rep-penalty; that's what we're seeing.

Real fix requires forking the sampler in session_api (~50 lines
of coffee). Half-day of work. Tabled per Mr. Hinds — pivoting to
puppeteer / HF-scan effort instead. If we come back, this is
where to start.

### LoRA-on-Jim question (asked, answered)

Would LoRA fix the loops? PARTIALLY:
- LoRA on Jim's stories reduces loop frequency (his prose is
  varied; model learns that distribution).
- LoRA does NOT insert repetition_penalty into the sampler.
- Best combo: LoRA-tuned Qwen3.5-Base + rep-penalty in
  session_api. Neither exists yet; both are recommended.

### State of the writer/pipeline codebase — pinned

- All safety brakes remain at their new (higher) defaults.
- mx.tidy L>1 gate is in place.
- Debug logging (per-layer spike, per-SDPA shapes) still on.
  Cheap; leave until we're bored of watching it.
- Norm.weight fold (yesterday's Hail Mary) is still in place;
  today's runs show it doesn't produce garbage, so the "revert
  the fold" hypothesis is disfavored.
- Do NOT re-litigate the GGUF/llama.cpp rejection.

### Do this in the morning

1. If picking Qwen3.5 back up: **fork the sampler** in
   `session_api.coffee` to add `repetition_penalty` (~1.15) and
   `top_k` (~40). That single change is the largest remaining
   quality lever.
2. If moving forward with model-shopping via puppeteer:
   `~/puppeteer/test.sh` (with `HF_TOKEN` in env) was never run —
   run it and inspect `out/hf_candidates.json`. See
   `~/puppeteer/GPT/advice/2026-08-27.md` for the full setup.
3. LoRA training on Huihui-Qwen3-4B-Instruct via `lora_ite` is
   still the fallback if HF-scan doesn't surface something better.
